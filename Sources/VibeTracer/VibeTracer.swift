import Foundation

/// Vibe Tracer SDK public entry point.
/// Full API arrives in Task 6.2; this placeholder keeps the module valid so
/// `swift build` succeeds throughout scaffold phases.
public enum VibeTracer {
    public static let version = "1.1.0"
}
